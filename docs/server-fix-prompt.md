# برومبت إصلاح السيرفر

النص الكامل موجود في الباك إند:

`../packand-home11/docs/server-fix-prompt.md`

استخدمه لإصلاح:
- CORS / `FRONTEND_URL`
- Gemini + Queue
- Reverb DNS / TLS / Proxy (المنفذ 443 → 8080)
- مواءمة `VITE_REVERB_*` مع الباك
